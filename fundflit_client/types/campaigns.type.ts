type Campaign = {
  id: string;
  name: string;
  campaign_name: string;
  story: string;
  goal: number;
  current_funds: number;
  futureDate: string;
  imageUrl: string;
  isPublic: boolean;
  shortURL: string;
};

export default Campaign;